@{
    Layout = "_AdminLayout";
}

<div class="container-fluid mt-4 mainContent" >

    <!-- Nội dung SPA sẽ được load vào đây -->
    <div id="mainContent">
        <div class="text-center mt-5">
            <div class="spinner-border text-primary"></div>
            <p class="mt-2 fw-bold">Đang tải dữ liệu...</p>
        </div>
    </div>

</div>

@section Scripts {
    <script>
        // Khi vào /Admin → tự load Dashboard
        window.onload = function () {
            loadPage("Dashboard");
        };

        // Hàm load partial từ server
        function loadPage(pageName) {

            fetch(`/Admin/${pageName}`)
                .then(res => res.text())
                .then(html => {
                    document.getElementById("mainContent").innerHTML = html;

                    // Active sidebar
                    document.querySelectorAll(".sidebar .nav-link")
                        .forEach(l => l.classList.remove("active"));

                    const target = document.querySelector(`[data-page='${pageName.toLowerCase()}']`);
                    if (target) target.classList.add("active");
                });
        }

        // Sidebar click → load partial
        document.querySelectorAll(".sidebar .nav-link").forEach(link => {
            link.addEventListener("click", e => {
                e.preventDefault();
                const page = link.dataset.page;

                // Chuyển "products" → "ProductList"
                const map = {
                    "dashboard": "Dashboard",
                    "products": "ProductList",
                    "categories": "CategoryList",
                    "orders": "OrderList",
                    "users": "UserList",
                    "contacts": "ContactList"
                };

                loadPage(map[page]);
            });
        });
    </script>
}
